{
  "name": "notekeeper",
  "version": "1.0.0",
  "private": true,
  "description": "NoteKeeper — notes, voice notes, to-do lists, reminders & pop-out notes in a Chrome side panel. Private, fast, offline.",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@wxt-dev/module-react": "^1.1.3",
    "typescript": "^5.6.0",
    "wxt": "^0.20.0"
  }
}
